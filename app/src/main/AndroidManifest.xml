<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rasti.selaraspos">

    <!-- ===== PERMISSIONS ===== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SelarasPOS"
        android:usesCleartextTraffic="true">

        <!-- ===== SPLASH SCREEN / LAUNCHER ACTIVITY ===== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="sensor"
            android:theme="@style/Theme.SelarasPOS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ===== LOGIN & REGISTER ===== -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== HALAMAN UTAMA (DASHBOARD) ===== -->
        <activity
            android:name=".Halaman_Utama"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== TRANSAKSI ===== -->
        <activity
            android:name=".activities.TransaksiActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== LAPORAN ===== -->
        <activity
            android:name=".activities.LaporanActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== AKUN ===== -->
        <activity
            android:name=".activities.AkunActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== PEGAWAI ===== -->
        <activity
            android:name=".pegawai.DataPegawaiActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".pegawai.ModPegawaiActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== CABANG ===== -->
        <activity
            android:name=".cabang.DataCabangActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".cabang.ModCabangActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== PRODUK ===== -->
        <activity
            android:name=".produk.DataProdukActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".produk.ModProdukActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== KATEGORI ===== -->
        <activity
            android:name=".kategori.DataKategoriActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".kategori.ModKategoriActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== PRINTER ===== -->
        <activity
            android:name=".activities.PrinterActivity"
            android:exported="false"
            android:screenOrientation="sensor" />

    </application>

</manifest>